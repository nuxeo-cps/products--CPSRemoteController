CPSRemoteController installs a new tool, the portal_remote_controller.

This new tool provides an high-level API for manipulating documents. This tool
is particularly useful for application which may use XML-RPC to communicate
with CPS.
